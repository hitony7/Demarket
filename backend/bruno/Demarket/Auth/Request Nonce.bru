meta {
  name: Request Nonce
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}{{apiPrefix}}/auth/request-nonce
  body: json
  auth: none
}

headers {
  content-type: application/json
}

body:json {
  {
    "walletAddress": "{{walletAddress}}"
  }
}
