name: Build and Validate

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend/Demarket

    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/Demarket/package-lock.json

      - name: Set up npm
        run: npm install -g npm@11.6.2

      - name: Install frontend dependencies
        run: npm ci

      - name: Type check frontend
        run: npx tsc -p tsconfig.app.json --noEmit

      - name: Build frontend
        run: npm run build

      - name: Upload frontend build artifacts
        if: success()
        uses: actions/upload-artifact@v6
        with:
          name: frontend-dist
          path: frontend/Demarket/dist/

  backend:
    name: Backend Smoke Check
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Set up npm
        run: npm install -g npm@11.6.2

      - name: Install backend dependencies
        run: npm ci

      - name: Smoke check route imports
        env:
          PINATA_API_KEY: test
          PINATA_API_SECRET: test
        run: node -e "import('./src/routes/index.js').then(() => console.log('backend routes ok')).catch((error) => { console.error(error); process.exit(1); })"
